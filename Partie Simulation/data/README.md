# Data and Artifacts

This directory stores generated artifacts and runtime outputs.

## Layout

- `outputs/`
  - training logs
  - checkpoints
  - best/final models
  - tensorboard event files
  - evaluation artifacts
  - videos

Keep large generated files here (not in source folders).
