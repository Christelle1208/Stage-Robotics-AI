# Projects

This directory contains first-party projects in this workspace.

## Projects

- `so100-mujoco-rl/`
  - Main codebase for SO-100 MuJoCo RL tasks, training scripts, and sim-to-real pipeline.
  - Start here for training and deployment work.

- `clean-pickdrop/`
  - Isolated pick-and-drop experiment project.
  - Its code is under `clean-pickdrop/app/`.

## Recommended working directory

For most current work:

```bash
cd "projects/so100-mujoco-rl"
```
