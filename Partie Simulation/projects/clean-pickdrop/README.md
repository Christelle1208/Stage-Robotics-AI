# Clean Pick-Drop Project

This project was separated from the main SO-100 RL project to keep the workspace clean.

## Structure

- `app/`: original `clean_pickdrop` codebase and its own README.

## Usage

```bash
cd "projects/clean-pickdrop/app"
python -m clean_pickdrop.train --algo ppo --timesteps 1000000
```
